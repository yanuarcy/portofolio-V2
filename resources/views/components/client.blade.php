<section class="clients">
    {{-- <h3 class="h3 clients-title">Clients</h3>

    <ul class="clients-list has-scrollbar">
        @foreach ($clients as $item)
            <li class="clients-item">
                <a href="#">
                    <img src="{{ asset('storage/' . $item->image) }}" alt="client logo" />
                </a>
            </li>
        @endforeach
    </ul> --}}
</section>
